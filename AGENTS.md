# AGENTS.md

## Environment

- 所有 `python` / `pytest` 命令使用 `conda run -n ship_detect ...`
- 依赖文件：`files/requirements.txt`
- 当前入口脚本还直接依赖 `Flask`、`Flask-CORS`、`onnxruntime`；执行前不要假设 `files/requirements.txt` 已完整覆盖运行时依赖

## Entry Points

- `image_main.py`：图片检测 HTTP 服务
- `image_main copy.py`：调试副本；除手动测试分支外，和 `image_main.py` 的请求处理逻辑应保持同步
- `video_main.py`：视频检测与跟踪入口
- 统一检测入口：`target_module/image_detect_module/target_detection.py` → `detect_targets(image_input, output_dir=None, enable_tracking=False)`

## Execution Rules

1. 涉及磁盘图片路径读写时，不要直接使用 `cv2.imread` / `cv2.imwrite`；使用 `cv_utils.imread_unicode()` / `imwrite_unicode()`。
2. 不要在调用处重复写死检测类别、模型路径、置信度阈值、NMS 阈值或图片输入输出目录；以 `target_module/image_detect_module/config.py` 的 `Config` 为准。
3. 修改 `image_main.py` 的请求解析、检测调用、结果图保存或返回结构时，同步检查 `image_main copy.py` 的对应逻辑；`image_main copy.py` 允许额外保留 `TEST_MODE` 调试分支。
4. 当前 `detect_targets()` 实际只支持图片路径字符串；不要把内存帧或 `np.ndarray` 直接传给它。
5. 不要假设 HTTP 成功响应等于检测结果；当前 `image_main.py` / `image_main copy.py` 中的 `APIResponse.success()` 未返回标准成功体。若修改图片 API 返回契约，需同步更新 `README.md`。
6. `video_main.py` 和端到端测试依赖外部服务或本地数据时，先确认 RTSP、RabbitMQ、模型文件和输入样本可用，再运行完整流程。
7. 修改视频默认输入源、RTSP 输出地址或默认输出文件时，统一改 `target_module/image_detect_module/config.py` 中的 `Config.VIDEO_RTSP_INPUT`、`Config.VIDEO_RTSP_OUTPUT`、`Config.VIDEO_OUTPUT_PATH`；不要在 `video_main.py` 重新写死。
8. 视频运行和 MOT 导出的 tracking 更新分发应共用 `target_module/image_detect_module/utils/tracking_update.py`；不要在入口脚本里重新实现 `_is_msdc_tracker()` / `_update_tracking_for_frame()`。
9. 正式评测可视化以 MOT txt 为准，使用 `tools/evaluation/render_mot_video.py`；不要恢复已删除的 `tools/evaluation/render_tracking_video.py` 作为正式链路。
10. 每次完成代码修改后都要更新文档 `README.md`。

## Formal Evaluation Reporting

- 每次“正式测试 / 正式评测 / full run / benchmark”完成后，必须同时交付以下结果；缺任一项不能宣称正式测试完成：
  1. 带标注的可视化视频，至少包含目标框、类别和 track ID；视频必须写入本次 run 的独立 `visualizations/` 目录，不覆盖历史结果。
  2. MOT 指标汇总，至少包含 `MOTA`、`IDF1`、`IDSW`、`FN`、`FP`；若已有 `HOTA`、`IDTP`、`IDFP`、`IDFN` 也一并汇报。
  3. 速度统计，至少包含总处理帧数、总耗时、平均单帧耗时和平均 FPS。
  4. 分阶段耗时，至少拆分为视频读取/解码、低阈值检测、高阈值框筛选、lifecycle tracker、可视化渲染、结果写盘/导出；不适用的阶段写 `0` 或 `N/A`，不要省略。
  5. 输出路径清单，包括 MOT txt、TrackEval summary、诊断 JSONL/CSV、可视化 MP4 和速度/耗时统计文件。
- 如果当前评测脚本没有生成速度或分阶段耗时，先补充计时输出或额外日志，再运行正式测试；不要只凭终端主观估算。
- 正式测试结果必须写入新的时间戳 run 目录；临时 smoke、失败中断、partial 输出要标清楚或清理，避免和正式结果混在一起。

## Reference

- 需要接口细节、目录说明或变更记录时，查 `README.md`

## 学术写作协作规则（MS-DC-ELT MOT 论文）

本节记录协助 HYJ 撰写 MOT 论文时必须遵守的写作规则。每次写作前先过一遍本节，提交内容前按文末“自查清单”逐项核对。

### 规则一：禁止编造伪学术术语

**问题表现**：习惯性堆砌“听起来专业”但业内并无对应说法的复合词，例如：“强不适定问题”“弱观测与强运动扰动”“一致轨迹标识”“observation不完备”“非平稳几何变化”“identity separability”“部分可观测的非平稳时变数据关联问题”“标准化观测”“轨迹生死判定”“脆弱性”。

**根因**：被指出造词后，往往只换掉那一个具体词，没有把“禁止造词”当作贯穿全文的硬约束。

**规则**：
- 任何复合术语，若不能在 MOT/CV 主流文献中找到对应说法，禁止使用。
- 优先用领域已确立的标准说法（如“数据关联”“ID switch”“fragmentation”“IoU”“reliability score”），其次才考虑直白描述，绝不自造新词。
- 写完后逐句自问：“这个词是哪篇论文在用，还是我编的？”

### 规则二：抽象描述必须落到具体定义

**问题表现**：“可判别的时序观测”——判别什么？观测什么？“标准化观测”——标准化了哪个字段、怎么标准化？

**规则**：
- 凡是名词性短语描述一个操作或对象，必须能在下一句话或括号内说清楚具体内容（字段、公式、判定条件、数值）。
- 如果一个词不展开就没有信息量，说明这个词本身有问题，应替换为具体陈述。

### 规则三：问题陈述（Q1/Q2）与方法论严格分离

**问题表现**：
- 把方法的命名（如“轨迹生命周期管理”）直接写进研究问题定义里。
- 把“现有方法依赖单帧置信度/固定阈值会失效”这类方法论批判提前写进 Q1。

**规则**：
- Q1/Q2 只描述研究领域客观存在的现象和难点，不出现任何与本文方法绑定的专有名词、模块名、机制名。
- 对现有方法的批判、解法的提出，留给“现有方法局限性”和“本文方法”部分。
- 自查：Q1/Q2 中出现的每一个名词，删掉本文方法后是否依然成立？若不成立，说明混入了方法论。

### 规则四：同一结论只陈述一次，不重复强调

**问题表现**：
- “本文方法不解决难点一/三”这句话在 Q4、Q5、Q6 中各说一遍。
- 同一个“分级判断”逻辑被拆成三个独立分点重复呈现。

**规则**：
- 范围声明、局限性声明等结论性表述，全文只在一个最合适的位置说一次（通常是首次确立范围时，或全文收尾的局限性说明处）。
- 写完一个章节后检查：是否有某句话在前面已经说过一遍意思相同的内容？若有，删除重复项，只保留信息量最大的一处。
- 多个分点描述前，先确认它们是否本质上是同一机制在不同环节的应用——若是，合并为一个分点，再说明其在不同环节的具体应用方式，而非拆成多个并列分点。

### 规则五：方法与难点的对应关系必须先核实代码/事实，再下结论

**问题表现**：在未查看实际代码的情况下，直接采信“模块 A 对应难点 1，模块 B 对应难点 3”的对应表，后经代码文档核实，发现该项目既无 GMC（全局运动补偿）也无 ReID/外观模块，原对应关系不成立。

**规则**：
- 遇到“方法 X 解决难点 Y”类强声明时，先要求查看代码/文档依据，再决定是否采信，不直接顺着表面对应关系往下写。
- 若核实后发现方法实际只覆盖部分难点，按“诚实收窄范围”处理（见规则六），不要勉强让方法“对应上”所有难点。
- 涉及具体技术细节（变量、公式、阈值、状态转移条件）时，优先引用已提供的代码/文档原文，不凭印象补全。

### 规则六：诚实收窄研究范围是正常操作，但表述方式要专业

**原则**：论文承认“只解决了部分难点”完全正常，不会破坏论证自洽性；真正破坏自洽性的是重复声明（见规则四）和语气上的犹豫感。

**规则**：
- 范围声明使用陈述性语气（“本文方法以此为核心攻克对象”），不用“暂不”“留作”“并未……”等带歉意/犹豫感的措辞重复铺陈。
- 局限性只在全文收尾处完整说明一次，格式参照顶刊惯例：“本文方法面向 X 设计，对 Y、Z 未作针对性处理，留作后续工作方向。”

### 规则七：禁止使用无来源的“而非……”对比句

**问题表现**：“而非在检测阶段直接舍弃……”“而非为两个环节分别设计独立规则”——这类句子在和一个从未被点名、不知道指代谁的假想做法做对比，本质是空洞修辞。

**规则**：
- 如果要做对比，必须明确对比对象是谁（某具体方法、某篇文献），不能用“而非……”虚指一个不存在的对照组。
- 默认直接陈述“本文做了什么、为什么这样做能针对性解决前面定义的难点”，不需要通过否定一个假想对象来凸显自己。

### 规则八：句子要精炼，避免同义重复

**问题表现**：“时序上连贯一致”——“跨帧”与“时序上”重复，“连贯”与“一致”重复。普遍存在“用一种说法讲一遍，再用近义词重复确认一遍”的写法。

**规则**：
- 每个修饰词只保留一个，写完后检查句子中是否有两个词在表达同一个意思。
- 优先用因果句（“A导致B”）代替“使得……从而……”的双重过渡结构。

### 规则九：技术问题（如“方法如何解决挑战”）必须给出具体公式/数值/判定条件

**问题表现**：第一版回答“方法如何解决挑战”时只给方向性描述（如“引入双置信度观测”），没有给出变量定义、公式、阈值、判定顺序，导致需要追问才补全。

**规则**：
- 涉及“方法如何解决/如何实现”类问题，默认直接给出：变量定义 → 公式/计算方式 → 具体判定条件/阈值 → 该机制解决的具体子问题。
- 不满足于“方向正确”的描述，必须落到可被代码或公式验证的颗粒度。

### 写作风格总原则

- 论文语气：客观陈述，不用对话感的解释性、修辞性语言（如“本质上”“可以说”“值得注意的是”等过渡词应尽量精简）。
- 分点命名参照顶刊 Method 部分习惯：名词短语，点明机制本质，不用句子或描述性短语作小标题。
- 全文术语前后必须一致：同一概念在 Q1 到 Q7 中只用一种说法指代，不中途换称呼（如不要“身份保持”和“数据关联”交替成为核心问题）。

### 提交前自查清单

1. [ ] 是否存在文献中找不到对应说法的自造术语？
2. [ ] 每个抽象名词是否都能具体说清楚指代什么？
3. [ ] 问题陈述部分是否混入了方法论或本文方法的专有名词？
4. [ ] 同一结论/同一机制是否被不同分点或不同问题重复表述？
5. [ ] 方法与难点的对应关系是否已用代码/文档核实，而非凭表面印象？
6. [ ] 局限性声明是否只出现一次，且语气陈述而非反复道歉？
7. [ ] 是否存在无具体对象的“而非……”虚指句？
8. [ ] 句子中是否有同义重复的修饰词？
9. [ ] “方法如何解决”类问题是否给出了具体公式/变量/阈值，而非停留在方向描述？
